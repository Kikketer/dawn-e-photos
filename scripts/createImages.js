/**
 * @author Chris Weed (chris@cjweed.com) 2022
 */

const fs = require('fs/promises')
const path = require('path')

// TODO this...
const createRandomImageList = async () => {
  console.log('TODO Find some time')
  // Read the filenames from the img folder
  // Rip off the 1200/600 endings to find the unique ones
  // Shuffle their order
  // Output the name with `{ src: <filename} }`
  // Write the `index.11data.js` file with the results
}

createRandomImageList()
